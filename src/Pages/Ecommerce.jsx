import { useStateContext } from "../Contexts/ContextProvider";

const DropDown = ({ currentMode }) => <di></di>;

const Ecommerce = () => {
  const { currentColor, currentMode } = useStateContext();

  return <div></div>;
};

export default Ecommerce;
